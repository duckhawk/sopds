Django==5.2.16
Pillow==11.3.0
# APScheduler: used by sopds_scanner (scheduled scans) and required by
# python-telegram-bot 21's JobQueue (needs >=3.10.4). 3.10.x accepts stdlib/
# zoneinfo timezones (3.6.x only accepted pytz, which crashed the bot on the
# container's local timezone) and pulls pytz/tzlocal itself. Keeps tzlocal<3.
apscheduler==3.10.4
# apscheduler still imports pkg_resources at load; Python 3.12 no longer
# bundles setuptools, and setuptools>=81 dropped pkg_resources.
setuptools==75.8.0
tzlocal==2.1
django-picklefield==3.4.0
lxml==6.1.1
# Telegram bot: python-telegram-bot 21+ (async, httpx-based). The sopds_telebot
# command was rewritten for this major version; it no longer pulls urllib3/six.
python-telegram-bot==21.11.1
# six is required by the vendored django-constance admin.
six==1.16.0
# OIDC (Keycloak) web login — see sopds_web_backend/oidc.py. Authlib's Django
# client uses requests for OIDC discovery and the token/userinfo calls.
Authlib==1.7.2
requests==2.34.2
psycopg2-binary==2.9.10
# Used by django.core.cache.backends.redis.RedisCache when REDIS_URL is set.
redis==5.2.1
uwsgi==2.0.28
sentry-sdk==2.19.2
mcp==1.2.0; python_version >= '3.10'
